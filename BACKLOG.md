# Loop Backlog — one item per iteration, top item first

Rules: small, testable, one screen/module each. Hermes rewrites raw wishes into this shape.
Format: `- [ ] item` → `- [x] item (preview: url)` or `- [ ] item [blocked: reason]`.

## Queue — V5 (Ashton's 2026-07-20 direction: Fitonist-style, liquid color, swim analytics, learning space)

Reference: https://dribbble.com/shots/24606569 (Fitonist) — near-black cards ~20px radius, lilac/yellow accents, per-card period toggles (Today/Week/Month), month calendar with activity dots, glowing curve charts with dot markers, bubble stats.

- [ ] **Liquid-glow color pass, app-wide**: no flat/static accent fills — rings become conic **gradients with soft outer glow** (fuel ring first), chart lines get gradient stroke + glowing endpoint dots, active states get gradient tints. Keep the perf rules: no backdrop-filter on grid cards, transform/opacity transitions only.
- [ ] **Fitonist-style desktop Home** (mobile keeps the compact stack): per-card period toggles, month calendar card with training-day dots, bubble-style macro/age-range-like breakdown for fuel, glowing curve charts. Reverse-engineer layout from the reference, adapt to RTD's data.
- [ ] **Model provider**: evaluate Nous Portal API (Hermes models, free tier) as the coach's LLM alongside/instead of Groq; keep whichever answers better, with fallback. No Telegram anywhere.

- [ ] Verify every V4 phase actually shipped (P1–P6 acceptance bars in REVAMP_V4_PROMPT.md); create one backlog item per gap found instead of fixing inline.
- [ ] Import parser: "kick"/"pull" markers still missing from pace-per-100 exclusion (w/u, w/d, drill already excluded) — two-word fix, queued for LOOP_PHASE3_PROMPT.md P4.
- [ ] Lighthouse mobile pass on /home and /analytics: performance ≥ 90; fix the top offender if below.
- [ ] Empty states audit: every empty card app-wide has a CTA that routes to where the data gets logged — partially done (Learn, Home cards); full app-wide sweep queued for LOOP_PHASE3_PROMPT.md P4c/P5c.

## Done

- [x] Coach tool-calling — the AI can log and edit (iteration 25): the in-app coach was read-only advisory-only ("the AI is the app" was the athlete's ask). New Groq function-calling loop (`callGroqChatWithTools` in `lib/ai/groq.ts`) plus a tools library (`lib/coach/tools.ts`, 13 tools: `log_water`, `log_meal`, `log_sleep`, `log_weigh_in`, `log_soreness`, `log_swim_session`, `log_swim_time`, `log_gym_set` with fuzzy exercise matching against today's session, `update_water_target`, `update_training_status`, `update_coach_memory`, `undo_last` (today-only, most-recent-of-kind), `get_today_summary`). The chat route runs the tool loop first, falls back to the existing advisory-only streaming path if tools are unavailable. Chat textarea now auto-grows 1-4 rows for pasting a whole swim session in one go. **Also fixed a real, pre-existing production reliability bug found while shipping this** — see the LOOP_LOG entry for the full trail: `getCoachAppContext()`'s 9-way `Promise.all` (plus 2 more concurrent queries at the route level, an 11-way total) against this project's deliberately tiny 3-connection pool could starve one arbitrary query indefinitely, hanging the whole coach chat endpoint past Vercel's 60s function cap with zero error surfaced to the athlete. Fixed two ways: `getAllMeetsWithEvents` (a genuine 2-round-trip anti-pattern, doubling its own connection pressure) rewritten as one left-joined query; every remaining concurrent query wrapped in a new shared `withFallback()` helper (`lib/db/withFallback.ts`) that degrades to an empty/null value after 10s instead of hanging forever. This bug predates this phase — it was always part of `getCoachAppContext`, just never exercised by this many concurrent tool-loop-triggered requests before.
- [x] Swim tab reorganized — AI-first logging, three views (iteration 24): `/swim` had accumulated ~14 cards in one long scroll (takeaway, volume, month dots, meet readiness, latest session, hero, meets list, add-meet form, manual session logger, session list, pace chart, time logger, recent times, PBs, split autopsy, time-to-15m, stroke counts) — the athlete called it a mess, and separately couldn't find the AI-powered session import (`ImportSessionButton`, which already existed and already handles free-text swim/gym sessions via one Groq call) because it was buried in tiny empty-state corners. Split into three URL-param-driven views (`/swim?view=log|meets|analysis`, default `log`, same Link-push pattern as `AnalyticsTabs`) via new `SwimViewSelector.tsx`: **Log** (new `SwimLogHero` — a prominent "Log a swim" card opening the AI import flow, with the manual load-only logger collapsed behind a "Log manually instead" disclosure — plus weekly volume, month dots, latest imported session, session list, pace chart); **Meets** (meet readiness, hero, meets list + add-meet form, time logger, new `RecentSwimTimesCard`); **Analysis** (new `SwimAnalysisView` — PBs vs goal, split autopsy chart, time-to-15m chart, stroke counts). The old monolithic `SwimSection.tsx` (which bundled ALL of the above into one file, the reason the whole thing got dynamically imported together in iteration 19) was deleted — its recharts-dependent half (split autopsy + time-to-15m) became `SwimAnalysisView.tsx`, still the only next/dynamic import site on this page, and everything else split into small direct imports since none of the rest ever touched recharts. Import parse failures no longer dead-end: the error state gained a **Save as unstructured** button (writes the raw text via the same `logSwimSessionAction` the manual logger uses, with a load-rating input) so a bad parse mid-poolside still logs something. `SwimMeetReadinessCard` no longer renders a wall of `— → 29.78 —` dashes when zero times are logged for a meet — collapses to one line + a link to log the first time.
- [x] Unit-aware train logging + honest set rows + rest-start cue (iteration 23): `settings.weightUnit` (kg/lb) existed end-to-end in the data layer (schema, SettingsForm, MCP route) but `ExerciseCard.tsx` hardcoded `kg` everywhere — Settings' unit toggle silently did nothing for the actual logging flow. New `lib/train/units.ts` (`kgToDisplay`/`displayToKg`/`unitLabel`) converts only at the display/input edge; storage, DB, e1RM, and tonnage math all stay kg, unchanged. `weightUnit` now threads `train/[phaseId]/page.tsx` → `WorkoutSession` → `ExerciseCard`: the ghost-weight placeholder, "Last session" summary lines, `SetRow`'s display text, and its edit-mode input all convert on the way out and back in. Also fixed the "Set 1 –kg × 6 @ RPE –" broken-looking display for weightless (checkbox-completed) exercises like box jumps: set rows now build their text from only the parts that exist (`formatSetParts`), so a bodyweight set reads "Set 1 · 6 reps" instead of a wall of dashes. Rest timer now announces itself: `navigator.vibrate?.(50)` fires at the "Add set" button's own click handler (a genuine direct JSX handler, not a prop-wrapper — matters for the React Compiler purity rule), and `RestPill` reads "Rest started — 3:00" for the first ~3 seconds of a rest before switching to the countdown, since the athlete finished sets with no idea a timer existed. Deliberately left as-is: the checkbox-complete flow still does not start a rest timer (only "Add set" does) — checking a box means "I did the whole prescribed thing," which doesn't need rest coaching.
- [x] Glass active pills, sliding-thumb drift bug deleted (iteration 22): every tab/pill selector (mobile dock, desktop TopBar nav, AnalyticsTabs, PeriodSelector, SegmentedControl) positioned its active-state pill as a SEPARATE absolutely-positioned div, computed via `calc(index/count * 100%)` — a formula that never accounted for the flex `gap` between items, so the pill drifted further off-center the higher the active index (visible in the athlete's screenshots as the white pill overhanging past the Analytics tab). Rewrote all five so the active item styles ITSELF (new shared `.rtd-pill-active` class in globals.css: `rgba(255,255,255,0.14)` glass fill + hairline border + backdrop blur + glow, `#fff` text) instead of positioning a sibling — structurally impossible to drift since there's no longer a second element to mis-position. Also replaces the flat solid-white-pill-with-black-text look the athlete has now flagged twice with the standing "no flat solid fills" direction. `SwimSessionLogger`'s solid-cyan active load buttons got the same glass treatment (tinted cyan fill + cyan border instead of solid `var(--rtd-cyan)` background). Swept the rest of the app for other `#0b0b0d`-on-solid instances — the remaining ones (`DeltaChip`, the swim `GapChip`, `MonthCalendarCard`'s trained-day dot, Learn's track-progress badge) are data/status badges, not selectable tabs, and correctly keep their colored-fill-plus-dark-text styling for legibility.
- [x] Mobile chrome collision fixes (iteration 21): the coach FAB rendered on every page including `/more/coach-ai` itself, sitting directly on top of that page's own Send button (the athlete's exact reported bug). `CoachPanel.tsx`'s mobile Link now checks `pathname` and renders nothing on that route. New `useKeyboardOpen` hook (`lib/useKeyboardOpen.ts`, `window.visualViewport` resize-based) hides the bottom dock, coach FAB, and RestPill while the iOS keyboard is open — previously the fixed dock floated mid-screen over whatever was behind the keyboard. `RestPill` also moved from centered to left-aligned on mobile (`left-4` instead of `left-1/2` translate-centered) with a `max-w-[calc(100vw-96px)]` cap, so it can no longer overlap the FAB's right-4 corridor during a rest. Main content's mobile bottom padding bumped 92px→96px for full dock clearance.
- [x] ACWR cold-start gate (iteration 20): `computeAcwr` returned a ratio of exactly 4.00 the instant any load existed with zero history older than 7 days (acute/7 == chronic/28 of the same data), which drove Home/Analytics/the readiness panel to scream "back off intensity" at any brand-new logger — this is exactly what happened to the athlete on their real first training day. Now `ratio` stays `null` until at least one day of logged load falls outside the acute (7-day) window. `loadTakeaway` explains the gap ("Building your load baseline — ACWR needs about two weeks...") instead of returning nothing; the readiness signal's null-ratio copy now says the same ("Building baseline — needs ~2 weeks of history.") instead of the vaguer "Not enough training history yet."
- [x] Protein adherence over-target flagged as success, not warning (iteration 20): the protein row lacked `overIsGood: true`, so 135% rendered an orange ProgressChip on a hardgainer phase where "eat more protein" is the entire point. kcal unchanged (large overshoot is still worth a flag there).
- [x] Duplicate % display in the improvement matrix (iteration 20): progress rows measured in `%` (protein, kcal) showed the same number twice — `"135 %"` as plain text next to a `[135%]` chip. The plain-text value now only renders for non-% progress rows (water, in L, still shows `4.5L` next to its `[150%]` chip since those are two different units of information).
- [x] Truncated "needs more data" hints on iPhone (iteration 20): every `needsDataHint` string in `improvementMatrix.ts` dropped its redundant "needs more data — " prefix (the UI already prepends "Needs data — " once, centrally); the row also switched from `truncate` (single-line ellipsis) to wrapping, so the full hint is always readable on narrow screens.
- [x] Error boundary self-heals once before showing the wall (iteration 20): `(app)/error.tsx` now attempts one silent 800ms auto-retry (`reset()`) on the first error in any 30-second window (tracked via `sessionStorage`) instead of immediately showing "Something went wrong" — most hits are a transient Supabase pooler hiccup, and the athlete hit this cold-opening the app on day one. A second error within the window shows the wall with clearer copy ("Still couldn't reach the database...").
- [x] Manual water amount entry (iteration 20): `WaterLogger.tsx` only had +250/+500/+750 preset buttons — no way to log "1L" or any other exact amount. Added an inline text input + Add button alongside the presets (accepts either liters like `1` or milliliters like `750`, disambiguated by a ≤10 threshold, clamped 1-5000ml).

- [x] Code-split recharts off the Overview tab (iteration 19): `SwimSection`, `LoadSection`, `PowerSection`, `BodyweightSection` now load via `next/dynamic` (SSR kept on) instead of static imports — the Overview tab's JSX never invokes any of them (they're each gated behind a specific `props.tab === "..."` block), so the recharts bundle is no longer forced to be reachable from every `/analytics` render regardless of which tab is open. Turbopack's `next build` doesn't print the classic first-load-JS-per-route table in this Next.js version, and the RSC client-reference-manifest lists every reachable client module (including dynamically-imported ones) without distinguishing eager vs. lazy — so no reliable before/after KB number could be produced with this toolchain (no bundle analyzer per the no-new-deps rule, and live network tracing needs an authed browser session which is off-limits). Verified structurally instead: read the source to confirm the Overview tab's render path never touches the 3 chart sections, and confirmed live that `?tab=overview` omits the train-tab's `detail-strength` markup while `?tab=train/fuel/recovery` and `/swim` each still render their respective chart sections correctly with zero regressions.
- [x] Lighthouse (iteration 19): best-effort desktop run on unauthenticated `/login` — Performance 99, Accessibility 94, Best Practices 100, SEO 91.
- [x] Soreness as a 4th readiness signal (iteration 18): `computeReadinessSignals` gains an optional `recentSoreness` input — omitted entirely (not a nag-yellow) when nothing was logged today/yesterday, red at 4-5/5, yellow at 3/5, green at 1-2/5. Wired into both call sites (Home hero, Recovery page), reusing the soreness query the Recovery page already had.
- [x] Chart endpoint-glow dots (iteration 18): `ComparisonLine`'s current series and `Sparkline` (opt-in `endDot` prop, enabled at StatCard + the improvement matrix's row sparklines) now end in a glowing filled dot on the last real point — the Fitonist look, finished off.
- [x] Empty-state CTA sweep (iteration 18): TrainingLoadCard/RecentPRsCard's empty states now link to `/train`; FuelAdherenceCard/RecoveryOverlayCard's "needs more data" lines link to `/fuel`/`/more/recovery`. App-wide grep swept for remaining dead-end strings — the improvement matrix's `needsDataHint` rows were already fully clickable (whole row is a Link to the tab), StatCard's generic internal empty state stays as-is (no single destination for a shared component), ComparisonLine's chart-internal "Needs more data" fallback stays as-is per the plan.
- [x] Meet readiness card on /swim (iteration 17): reuses the same trend-fit readiness math the coach already had (`lib/swim/readiness.ts`'s `computeMeetReadiness`, unchanged), now surfaced visually — one block per upcoming meet (max 2, omitted entirely when there's none), event rows show current best / target / a DeltaChip-style gap pill. Verified live against the real NCAA Philippines meet (Dec 4, 5 events, all `currentBestMs: null` since no matching swim times are logged yet under those exact event names) via the `get_meet_readiness` MCP tool before writing the live-HTML assertion.
- [x] Pace exclusion completeness (iteration 17): `lib/swim/pace.ts`'s `EXCLUDE_MARKERS` was missing "kick" and "pull" (w/u, w/d, drill were already excluded) — two-word fix, verified with a throwaway tsx script showing a mixed kick/pull/free interval set now averages only the free interval.
- [x] Swim empty-state CTAs (iteration 17): the weekly-volume and latest-imported-session cards' empty states were plain sentences with no way to act — both now include an inline `ImportSessionButton` (compact) so the fix is one tap away instead of a dead end.
- [x] Train rest timer v2 (iteration 16): the old per-card timer counted UP and vanished the moment a card collapsed or scrolled off. Rest state is now lifted to `WorkoutSession` (one shared timer, one 1s interval for the whole session instead of one per card) and counts DOWN to the exercise's prescribed rest, flipping to "+m:ss over" past zero. New sticky `RestPill` stays visible above the dock regardless of scroll/card state, buzzes (`navigator.vibrate`) and pulses once at zero, and auto-dismisses 5 minutes past target if left untapped.
- [x] Coach memory visible/editable in Settings (iteration 15): new "Coach memory" card on `/more/settings` — read-only view of the athlete model with an Edit → textarea → Save/Cancel flow (`CoachMemoryEditor.tsx`), backed by a new `updateAthleteModelAction` server action using `onConflictDoUpdate` (unlike the daily auto-refresh's `onConflictDoNothing`) so a manual correction today always wins. No schema change — same `ai_takeaways` table, same `(date, key)` unique index.
- [x] Coach reachability (iteration 15): mobile now has a floating coach button (previously desktop-only) linking to the full-screen `/more/coach-ai`, plus a "Coach" entry added to all 3 places the rest of the nav lives (Home's more-row, the avatar menu, `/more`'s link list) — it was reachable only via the desktop panel or a buried nav path before.
- [x] QuickLogSheet Escape-key close (iteration 15): small keyboard-a11y gap, same pattern CoachPanel already used.

- [x] Self-host Inter font (iteration 14): `next/font/google` needed Google reachable at build time — one flaky network = failed deploy (it happened in loop 4). Now `next/font/local` loading `src/app/fonts/InterVariable.woff2` (rsms.me's official variable build), zero build-time network dependency.
- [x] Coach panel chat history (iteration 14, real bug found in phase-3 audit): `CoachPanel.tsx`'s desktop slide-over hardcoded `initialMessages={[]}` on every open despite full history existing in the DB and being used server-side by the POST route to build prompt context — so it visually looked amnesiac even though the coach itself remembered everything. Added a `GET /api/coach/chat` handler + panel now fetches history on open before rendering `CoachChat`.
- [x] Service worker runtime cache bound (iteration 14): `RUNTIME_CACHE` grew forever on a daily-driver PWA; now trimmed to 60 entries after every write. `CACHE_VERSION` bumped to `rtd-v2` so old unbounded caches get purged via the existing activate handler.
- [x] Fuel meal-row delete affordance on mobile — verified already implemented (phase-3 audit): `MealSlot.tsx` renders an always-visible ✕ with `rtd-tap-target` on every logged item, mobile and desktop, gated only by `readOnly`. No code needed.
- [x] Water +500ml quick-log verify — verified already implemented (phase-3 audit): `QuickLogSheet.tsx`'s `logWater()` shows an inline ✓ for 1.5s and does not close the sheet. Already the wished behavior.
- [x] Analytics month view offset past data start — dropped, not deferred (phase-3 audit): the improvement matrix now uses trailing windows (iteration 10), which are offset-independent and always have data; each detail chart already renders its own "needs more data" state. No fix needed.

- [x] Persistent coach memory (iteration 13, the "Hermes memory" ask): a durable athlete model (soreness patterns, exercise preferences, schedule constraints, nutrition habits, injury history, motivators) now persists via the existing `ai_takeaways` table (key `athlete-model`, no schema change) and refreshes once/day, piggybacked on the daily brief's first-generation-of-the-day gate. Injected as an `ATHLETE MODEL (persistent):` section into both the coach chat's system prompt (`lib/coach/context.ts` → `route.ts`) and the daily brief's system prompt (`lib/coach/dailyBrief.ts`). Groq/DB failures keep the previous model and never block the brief. No Telegram, no schema migration.

- [x] Typography scale + PWA hygiene (iteration 12): Home hero's countdown number and readiness word now use the shared `.rtd-big-num` scale (34px→44px) instead of ad-hoc clamp()/text-large-title sizing; StatCard's headline number steps up from 32px to 44px on desktop (mobile stays 28px per V4 density). PWA audit found everything already correct on inspection — manifest already had 192+512 maskable icons, `theme_color`/`background_color` both `#000000`, root layout's Viewport API already emitted the `<meta name="theme-color">` tag, `/offline` already existed and worked — so the only change needed was adding `/offline` to smoke.mjs's permanent route list as a regression guard.

- [x] Home single render (iteration 11): merged the desktop 12-col bento grid and the mobile stack into one `.rtd-home-grid` container (flex-column below md, the same 12-col grid at md+) so every module renders exactly once instead of twice with one copy CSS-hidden. Visual order differs by breakpoint (fuel/stat cluster sits mid-sequence on mobile, near the top on desktop) so every child got explicit `order-N md:order-M` classes rather than relying on default/tied ordering. Fuel+Bodyweight+Consistency share one wrapper (`grid grid-cols-2 gap-2.5 md:contents`) so they're a compact 2-col cluster on mobile and three independent 12-col-grid items on desktop. MonthCalendarCard is `hidden md:flex` (desktop-only, as before — it never had a mobile equivalent). 6 card components (HomeHeroBand, MonthCalendarCard, TodaysPlanCard, CoachBriefCard, WeekMapCard, TrainingLoadCard, RecentPRsCard) gained a `className` passthrough prop to carry the order classes.

- [x] Improvement matrix trailing windows (iteration 10, the Monday-morning fix): current/previous/delta/progress/needsDataHint now compare a trailing N-day window ending today (7 for week, 28 for month) against the equal-length window before it, instead of calendar-period-to-date. A Friday session now counts on the following Monday. Sparkline/dot history unchanged (still calendar-period-aligned via periodStarts, still respects the offset stepper). Card label now reads "Improvement matrix · last 7/28 days vs previous".

- [x] Learn progression polish + coach hook (iteration 9): first incomplete level per track gets an outline ring + full-white title (later incomplete levels dim to secondary text); track cards on /learn show an "Up next: {level}" caption ("Track complete 🎉" when done); each level row gets a sparkle button that deep-links to the coach with a pre-filled "teach me this + give me an exercise tonight" prompt. Also fixed a false premise in LOOP_PHASE2_PROMPT.md — the coach page did NOT already accept `?q=`; added real support (CoachChat auto-sends an `autoSend` prop once on mount, coach-ai/page.tsx reads `searchParams.q`).

- [x] Learn tab (career learning space, iteration 8): new nav section with 5 GitHub curricula as leveled tracks — 30 Days of Python (Asabeneh), ML for Beginners (Microsoft), Build Your Own X (codecrafters-io), Project Based Learning (practical-tutorials), Cybersecurity (farhanashrafdev/90DaysOfCyberSecurity repacked into 11 levels). `/learn` shows all 5 tracks with progress chips; `/learn/[trackId]` lists levels with a tappable complete-toggle (server action, `learn_progress` table) separate from the external GitHub link tap target. Nav entry added to desktop TopBar, mobile MoreMenuButton, and /more.

- [x] Analytics tabs + Swim tab (iteration 5): pill tab bar Overview | Train | Swim | Fuel | Recovery (URL-param driven, server-rendered); Overview = matrix + per-domain takeaway link cards; Swim tab = weekly volume bars (gradient+glow), month dot calendar with legend (Fitonist pattern), latest imported session's interval breakdown, plus the existing SwimSection. Matrix rows now deep-link to their tab. Bodyweight lives with Fuel.

- [x] Cache-invalidation audit (iteration 3): every write path already busts both caches — in-app server actions call `updateTag("home-data")`+`updateTag("analytics-data")` (fuel/train/import/analytics/more actions), MCP route calls `revalidateTag(..., {expire: 0})` after all 15+ write tools. Hermes/Claude Telegram logs WILL appear in the UI immediately. No fix needed.

- [x] Smoke suite (iteration 2, adapted from the Playwright plan): `npm run smoke` boots the prod build (or targets a deployed origin via `SMOKE_BASE_URL`), mints a session with the app's own JWT signing (secret never printed), asserts every screen returns 200 with real content and no error boundary, plus auth-redirect still enforced. Zero new dependencies.
